Note that while this example contains run_solver.sh, it's an artifact of the mallob submission from 2025.

For 2026 competition submissions, the only files submitted should be the Dockerfile and solver_cmd.py. 
The run_solver.sh file should simply be part of the solver repo cloned into the Docker image.
