
Copy `solver_shim_cmd.py` to `docker/painless-images/`,
and copy all files in `leader/` to `docker/painless-images/leader/`.

Note that the name for this solver in `config.yml` needs to be `painless1`.